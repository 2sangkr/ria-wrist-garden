# 모든 아이의 모든 예술 — 현재 상태 (2026-05-25 기준)

## 사이트 개요

- **URL**: https://mymomo.gallery
- **사이트명**: 모모갤러리 — 모든 아이의 모든 예술
- **목적**: 어린이 작가들의 작품을 온라인 갤러리로 전시

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| 프레임워크 | Next.js 16 (App Router, TypeScript) |
| 스타일 | Tailwind CSS |
| DB | Supabase (PostgreSQL) |
| 이미지 스토리지 | Vercel public/images/ (기존) + Supabase Storage (신규 업로드) |
| 배포 | Vercel (GitHub 자동 배포) |
| GitHub | https://github.com/2sangkr/ria-wrist-garden |

---

## 페이지 구조

| URL | 설명 |
|-----|------|
| `/` | 홈 — 배너 + 작가 그리드 (모집 중 슬롯 포함) |
| `/works` | 전체 작품 목록 (모든 작가 작품 타임라인) |
| `/about` | 소개 페이지 |
| `/artist/[slug]` | 작가 갤러리 (나이별 그룹) |
| `/artist/[slug]/[workSlug]` | 작품 상세 페이지 |
| `/admin` | 관리자 로그인 |
| `/admin/dashboard` | 관리자 대시보드 |

---

## 현재 작가 현황

| 작가 | slug | 작품 수 | 스타일 |
|------|------|---------|--------|
| D.hee | dhee | 26개 | 핸드메이드, 드로잉, 아크릴 물감 |
| James | james | 6개 | 레고 조립, 종이접기 |
| Emma | emma | 5개 | 아크릴 물감, 색연필 |
| (모집 중) | — | — | 슬롯 3개 표시 중 |

---

## 주요 기능

### 공개 기능
- **작가 갤러리**: 나이별/연도별 그룹핑, 이미지 그리드
- **작품 상세**: 이미지, 제목, 날짜(나이·연도), 재료, 작가 정보
- **카카오톡 공유**: Kakao SDK 연동, 인앱 브라우저에서 카카오톡으로 공유
- **URL 복사/네이티브 공유**: 카카오 미지원 환경 폴백
- **작가 등록 문의**: 구글폼 링크 연결

### 어드민 기능 (`/admin`)
- 비밀번호 로그인 (쿠키 기반 세션)
- 작가별 작품 목록 확인
- 작품 추가 (제목, 날짜, 나이, 재료, 이미지 업로드)
- 작품 삭제 (이미지 파일도 함께 삭제)
- 이미지 → Supabase Storage 자동 업로드

---

## Supabase DB 구조

### artists 테이블
```
id (text PK), slug, name, tags (text[]), bio,
profile_color, is_empty (bool), sort_order
```

### works 테이블
```
id (serial PK), artist_id (FK → artists.id),
slug, title, image, materials (text[]),
created_at (date), age (int)
```

### work_likes 테이블
```
work_id (FK → works.id)
```

### page_views 테이블 (2026-05-25 추가)
```
id (bigserial PK), path (text), created_at (timestamptz)
```
- 홈/작가 페이지 방문 시 자동 기록
- 브리핑 방문수 집계에 사용

### 권한 설정
- `GRANT SELECT ON artists TO anon;`
- `GRANT SELECT ON works TO anon;`
- RLS policy: public_read_artists, public_read_works (SELECT TO anon USING (true))
- 쓰기는 service_role key만 가능 (어드민 API에서 사용)

---

## 환경변수 (Vercel)

| 변수명 | 용도 |
|--------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 프로젝트 URL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 공개 읽기용 키 |
| `SUPABASE_SERVICE_ROLE_KEY` | 쓰기용 서버 전용 키 |
| `ADMIN_PASSWORD` | 어드민 로그인 비밀번호 |
| `TELEGRAM_BOT_TOKEN` | GalleryBriefBot 토큰 |
| `TELEGRAM_CHAT_ID` | 브리핑 수신 채팅 ID |
| `TELEGRAM_WEBHOOK_SECRET` | 웹훅 검증 시크릿 |
| `CRON_SECRET` | 크론 인증 시크릿 |
| `METRICS_SECRET` | /api/metrics 접근 키 |

---

## 이미지 경로 구조

- **기존 작품**: `/images/dhee-work-1b.jpg` 등 (Vercel public/images/ 폴더)
- **신규 업로드**: `https://iqcahpgjflkauovccqwf.supabase.co/storage/v1/object/public/works/...`
- next.config.ts에 Supabase Storage remotePatterns 등록 완료

---

## 계정 정보

→ `_notes/프로젝트_작가님/06_계정관리.md` 참고

---

## 운영 관리

→ `_notes/관리운영/봇_운영.md` 참고

---

## 알려진 이슈 / 개선 필요 사항

- 어드민 작품 수정 기능 미구현 (현재 삭제 후 재등록으로 대체)
- 어드민 작가 추가/수정 기능 미구현
- 업로드 알림 — Supabase Webhook 미설정 (설정 방법은 봇_운영.md 참고)
- soso.gallery 브리핑 봇 연동 미완료
