# 작업일지 — 2026-05-05

## 오늘 한 일

### Supabase DB 연동
- Supabase 프로젝트 생성 (ID: `iqcahpgjflkauovccqwf`)
- `artists` / `works` 테이블 생성
- 기존 하드코딩 데이터(artists.ts) → DB migration 완료
- Vercel 환경변수 4개 추가:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `ADMIN_PASSWORD` (= `momo2026`)
- **RLS 이슈 해결**: GRANT SELECT ON artists TO anon; / GRANT SELECT ON works TO anon; 필수
- `src/lib/data.ts` 생성 — Supabase fetch 함수 (getArtists, getArtistWithWorks, getAllArtistsWithWorks)
- 모든 페이지 `force-dynamic` 적용 (DB 데이터 항상 최신 반영)

### 웹 어드민 페이지
- URL: `/admin` (비밀번호: `momo2026`)
- 기능: 작가별 작품 추가 / 삭제 / 이미지 업로드
- Supabase Storage 버킷 `works` — 작품 업로드 시 자동 생성
- 이미지 URL 형식: `https://iqcahpgjflkauovccqwf.supabase.co/storage/v1/object/public/works/...`
- 기존 작품 이미지는 Vercel public/images/ 에 있음 (두 가지 경로 병존)
- 미들웨어 버그 수정: 로그인 API(/api/admin/login)를 인증 예외로 처리

### 기타
- 푸터에 "관리자" 링크 추가 (/admin)
- 작가 등록 문의 구글폼 링크 교체 (새 폼으로 업데이트)
- 임시 디버그 API `/api/debug` 추가 (연결 진단용, 추후 삭제 권장)

---

## 현재 작가/작품 현황 (DB 기준)

| 작가 | slug | 작품 수 | 비고 |
|------|------|---------|------|
| D.hee | dhee | 26개 | 핸드메이드, 드로잉, 아크릴 |
| James | james | 6개 | 레고, 종이접기 |
| Emma | emma | 5개 | 아크릴, 색연필 |

---

## 트러블슈팅 기록

| 문제 | 원인 | 해결 |
|------|------|------|
| 작가 데이터 안 나옴 | Supabase anon 읽기 권한 없음 | GRANT SELECT ON artists TO anon; |
| 로그인 실패 (Unauthorized) | 미들웨어가 /api/admin/login 차단 | 로그인 라우트 미들웨어 예외 처리 |

---

## 다음 할 일 후보

- [ ] /api/debug 임시 라우트 삭제
- [ ] 어드민 — 작품 수정(편집) 기능
- [ ] 어드민 — 작가 정보 수정 기능
- [ ] 좋아요 기능 (Supabase DB 활용 가능)
- [ ] 새 작가 등록 플로우 (어드민에서 직접)
- [ ] 카카오 봇 연동 (사진 → 자동 작품 등록)

---

## 다음 작업일지

[[2026-05-06]]
